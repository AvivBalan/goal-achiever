import users from './users';
import {setAuthToken, clearAuthData} from './client';
import goals from './goals';
import steps from './steps';

export default {
  users,
  setAuthToken,
  clearAuthData,
  goals,
  steps
};