{
  "goalAchieverDb" : {
    "name": "db",
    "connector": "mongodb",
    "host": "${MONGODB_HOST}",
    "port": "${MONGODB_PORT}",
    "user": "${MONGODB_USER}",
    "password": "${MONGODB_PASSWORD}",
    "database": "${MONGODB_DATABASE}"
  }
}
